# Contoh secrets untuk Streamlit.
# Salin file ini menjadi `.streamlit/secrets.toml` (sudah di-gitignore → aman, tak ikut commit),
# lalu isi dengan API key OpenRouter Anda.
OPENROUTER_API_KEY = "sk-or-xxxxxxxxxxxxxxxxxxxxxxxx"
